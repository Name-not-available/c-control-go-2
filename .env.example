# Telegram Bot Configuration
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here

# API Provider Configuration
# Options: "google" (default) or "osm" (OpenStreetMap - FREE)
# Set to "osm" to use free OpenStreetMap API (no costs!)
API_PROVIDER=google

# Google Maps API Configuration (only needed if API_PROVIDER=google)
GOOGLE_MAPS_API_KEY=your_google_maps_api_key_here
